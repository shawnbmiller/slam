// an news object that contains title, description, and url
export default class Slam {
    constructor(slam) {
        this.title = slam.title;
        this.description = slam.description;
        this.url = slam.url;
    }
}